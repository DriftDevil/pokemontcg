# Git files
.git
.gitignore

# Node modules
node_modules

# Next.js build outputs
.next
out

# Local environment files (should be managed by .env, not committed)
.env*.local
!.env.example

# Docker specific files (if they are in the context but not needed for image)
Dockerfile
docker-compose.yaml
docker-compose.*.yaml
.dockerignore

# IDE and OS specific files
.vscode
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Test coverage
coverage

# TypeScript build info
*.tsbuildinfo

# ESLint cache
.eslintcache

# Project documentation and hosting config (if not needed in build context)
README.md
apphosting.yaml
